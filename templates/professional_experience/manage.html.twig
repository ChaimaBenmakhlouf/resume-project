{% extends "base.html.twig" %}

{% block body %}
<div class="row justify-content-center">
    <div class="col-12 col-lg-10 col-xl-8">
        <div class="card">
            <div class="card-header d-flex justify-content-between align-items-center">
                <h1 class="h4">List of Professional Experiences</h1>
                <a href="{{ path("professionalExperience_create") }}" class="btn btn-primary"><i class="fas fa-plus"></i>Create</a>
            </div>
            <table class="table table-striped table-hovered mb-0">
                <thead>
                    <tr>
                        <th class="text-center text-uppercase">Id</th>
                        <th class="text-center text-uppercase">Title</th>
                        <th class="text-center text-uppercase">Place</th>
                        <th class="text-center text-uppercase">Mission statement</th>
                    </tr>
                </thead>
                
                <tbody>
                    {% if app.user %}
                    {% for professionalExperience in app.user.professionalExperiences %}
                    <tr>
                        <td class="text-center align-middle">{{ professionalExperience.id }}</td>
                        <td class="text-center align-middle">{{ professionalExperience.title }}</td>
                        <td class="text-center align-middle">{{ professionalExperience.place }}</td>
                        <td class="text-center align-middle">{{ professionalExperience.missionStatement }}</td>
                        <td class="text-center align-middle">
                            <a href="{{ path("professionalExperience_update", { id: professionalExperience.id }) }}" class="btn btn-warning"><i class="fas fa-edit"></i>Edit</a>
                            <a href="{{ path("professionalExperience_delete", { id: professionalExperience.id }) }}" class="btn btn-danger"><i class="fas fa-trash"></i>Delete</a>
                        </td>
                    </tr>
                    {% else %}
                    <tr>
                        <td colspan="5" class="text-center align-middle">No professional experience</td>
                    </tr>
                    {% endfor %}
                    {% endif %}
                </tbody>
            </table>
        </div>
    </div>
</div>
{% endblock %}